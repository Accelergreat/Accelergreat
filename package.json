{
  "name": "accelergreat-docs",
  "version": "1.0.0",
  "description": "Accelergreat Documentation Site",
  "scripts": {
    "build": "node build-docs.js && npm run copy-assets",
    "copy-assets": "mkdir -p _site/styles && cp src/accelergreat_icon.png _site/ && cp favicon.ico _site/ 2>/dev/null || true",
    "clean": "rm -rf _site",
    "serve": "npx serve _site"
  },
  "dependencies": {
    "marked": "^4.3.0"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}